import { Route } from '@angular/router';
import { AdverpizingComponent } from './adverpizing.component';

export const AdverpizingRoutes: Route[] = [
    {
        path: 'adverpizing',
        component: AdverpizingComponent
    }
];
